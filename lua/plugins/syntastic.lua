return {
  {
    lazy = false, -- make sure we load this during startup if it is your main colorscheme
    "vim-syntastic/syntastic",
    -- event = "BufReadPost", -- You can load it on demand, e.g., when reading a buffer
  },
}
